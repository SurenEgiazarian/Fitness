import styled from "styled-components";

export const logo = styled.img`
  width: 220px;
  height: 35px;
`;

export const descCommon = styled.p`
  font-size: 20px;
  line-height: 115%;
  letter-spacing: -0.05px;
  font-feature-settings: "pnum" on, "lnum" on;
  color: #ffffff;
  opacity: 0.5;
  padding-bottom: 17px;
`;

export const mainHeader = styled.h2`
  font-size: 60px;
  line-height: 91.5%;
  letter-spacing: -1.16878px;
  font-feature-settings: "pnum" on, "lnum" on;
  color: #f4f4ff;
  padding-bottom: 52px;
`;

export const sticker = styled.img`
  width: 212.27px;
  height: 151.74px;
  transform: rotate(15deg);
  position: relative;
  left: 52px;
`;
